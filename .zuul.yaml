---
- project:
    templates:
      - build-openstack-sphinx-docs-python3
      - api-ref-jobs
    check:
      jobs:
        - build-openstack-releasenotes
        - openstack-tox-pep8:
            voting: false
        - openstack-tox-linters
    gates:
      jobs:
        - openstack-tox-linters
